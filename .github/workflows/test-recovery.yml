name: Test Actions Recovery

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-22.04
    timeout-minutes: 2
    
    steps:
      - name: Test if Actions is working
        run: |
          echo "✅ GitHub Actions is working!"
          echo "Timestamp: $(date)"
          echo "Runner: ubuntu-22.04"